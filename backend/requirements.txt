# Dependencias de la API de SomnoAI.
#
# La PRIMERA línea es el modelo empaquetado (Taller 6): el .whl trae el
# artefacto entrenado, su preprocesamiento y sus propias dependencias fijadas
# (numpy, scipy, pandas, scikit-learn, mne). Actualizar el modelo = reconstruir
# el wheel, cambiar esta línea y reconstruir la imagen. Sin tocar código.
./model-pkg/edad_cerebral-0.2.0-py3-none-any.whl

# ── API ──────────────────────────────────────────────
fastapi==0.141.1            # framework de la API (contrato en docs/API_CONTRACT.md)
uvicorn[standard]==0.52.4   # servidor ASGI (backend/run.sh)
python-multipart==0.0.32    # carga de archivos multipart (POST /predict)

# numpy, scipy, pandas, scikit-learn y mne NO se listan aquí: los fija el wheel del
# modelo, y duplicarlos con otra versión rompería la coherencia entre entrenar y servir.
#
# yasa y lightgbm se retiraron: la estadificación automática ya no se usa. El modelo
# exige hipnograma anotado (ver analysis.py), así que auto_yasa quedó inalcanzable y
# eran ~250 MB de imagen sin función. Para reactivarla: volver a añadirlos aquí, volver
# a instalar libgomp1 en el Dockerfile, y levantar el rechazo de analysis.py.
